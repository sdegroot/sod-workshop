/* eslint-disable @typescript-eslint/no-unused-vars */
import { z } from "zod";

// In this challenge, you'll create a function "validateEmail" that validates an email address.
// It does not demonstrate the power of Zod but it will get you familiar with the basics.

type Email = string;

export function validateEmail(email: Email): Email {
    throw new Error("Not implemented")
}